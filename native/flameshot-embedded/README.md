# Codex++ Embedded Flameshot

This CMake wrapper links selected Flameshot C++/Qt components into a shared
library loaded by the Codex++ launcher. The file also satisfies QHotkey's CPack
README lookup when QHotkey is consumed as a nested CMake project.
